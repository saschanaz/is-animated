/**
 * Checks if buffer contains GIF image
 *
 * @param {import('../file_handle').FileSource} source
 * @returns {boolean}
 */
export function isGIF(source: import('../file_handle').FileSource): boolean;
/**
 * Checks if buffer contains animated GIF image
 *
 * @param {import('../file_handle').FileSource} source
 * @returns {boolean}
 */
export function isAnimated(source: import('../file_handle').FileSource): boolean;
