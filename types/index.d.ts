/**
 * Checks if buffer contains animated image
 *
 * @param {import('./file_handle').FileSource} source
 * @returns {Promise<boolean>}
 */
export default function isAnimated(view: any): Promise<boolean>;
